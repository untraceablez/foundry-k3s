apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default-l2-advert
  namespace: metallb-system
spec:
  ipAddressPools:
  - default-pool
  # Optional: Specify which network interfaces to use for advertisement
  # interfaces:
  # - eth0
  # - ens18
